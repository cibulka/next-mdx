const plugin = require('tailwindcss/plugin')

/* Result */
const result = {
  darkMode: 'class', // or 'media',
  content: [
    './src/**/*.tsx', 
    './src/**/*.ts', 
  ]
};

module.exports = result;
